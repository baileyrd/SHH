{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core APIs (events, window control) needed by the host manager and terminal.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
